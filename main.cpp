#include "headers/task_1.hpp"
#include "headers/task_2.hpp"
#include "headers/task_3.hpp"
#include "headers/task_4.hpp"
#include "headers/task_5.hpp"
#include "headers/task_6.hpp"
#include "headers/task_7.hpp"
#include "headers/task_8.hpp"
#include <iostream>
#include <string>
#include <vector>

int main(){
    //std::vector<std::string> M = {"23:34", "01:23", "03:15"};
    //std::cout <<
    //choosing_cleaner(6, 10);
    //path_exist(1, 2, 5, 10, 8);
    //equation("x+2x+1-2=6+x-1");
    //min_dif(M);
    //path_finder(7339, 25);
    //calc_expression("-7+ ( ( 10 - 19) -9 )+5 -(3 + 1)  ");
    //numbers_from_range(10);

    /*
    std::vector<std::vector<int>> all_bushes;
    for (int i = 1; i < 4; i++){
        std::vector k {i, 1};
        all_bushes.push_back(k);
    }
    for (int i = 1; i < 4; i++){
        std::vector k {3, i};
        all_bushes.push_back(k);
    }
    for (int i = 3; i > 0; i--){
        std::vector k {i, 3};
        all_bushes.push_back(k);
    }
    for (int i = 3; i > 1; i--){
        std::vector k {1, i};
        all_bushes.push_back(k);
    }
    //all_bushes.push_back(std::vector{2,2});
    std::vector<std::vector<int>> perimeter_bushes = bushes(all_bushes);
    for (int i = 0; i < perimeter_bushes.size(); i++){
        std::cout << perimeter_bushes[i][0] << " " << perimeter_bushes[i][1] << std::endl;
    }
    */

    return 0;
}